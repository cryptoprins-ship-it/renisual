# Sample photos: Woonboten

Drop facade photos of houseboats (1600px wide, JPG, < 500KB) into this folder,
then add an entry to `index.json`:

```json
[
  { "file": "amsterdamse-ark.jpg", "label": "Amsterdamse ark" }
]
```

The `/render` page picks them up automatically via the "Voorbeeld" tab.
